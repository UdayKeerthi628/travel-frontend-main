services:
  frontend:
    build:
      context: .             # 👈 current folder (TRAVEL-FRONTEND)
      dockerfile: Dockerfile
    container_name: travel-frontend
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped
